# 2026-09-24

## 设置卡片材质：蓝牙列表重复柔光 + 通用卡片路由 + 弹窗玻璃

### 蓝牙重复柔光玻璃（承接上一轮的修复）
jadx 反查后确认根因不是「hook 开关」，而是 `view_corner` 被当成两种角色：
`BluetoothDevicePreference` 对**已保存设备**和**可用设备**复用同一个布局
`preference_bt_icon_corner`，区别只在 `ConnectPreferenceHelper` 写进 `view_high_light_root`
的高亮面是否保留（可用设备行在绑定末尾 `setBackground(null)` 就地清空）。
`SettingsCardBackgroundHook.standaloneTarget()` 的 `view_corner` 分支新增
`bluetoothRowOwnsSurface()` 判定 + `releaseStandalone()` 撤回材质，不再需要黑名单。

### 本次新增
- `appearance/CardSurfaceDetector.kt`（新）：内容驱动的通用「独立卡片表面」判定。
  规则：尺寸 → **background 非空**（没有背景的列表行直接排除）→ 背景确实不透明
  （CardView 以 `getCardBackgroundColor()` 为准，蓝牙 `view_corner` 是 transparent）→
  名称负向词 → 卡片形状（圆角/卡片类）→ 同条目内没有更外层卡片面。
- `SettingsCardBackgroundHook`：`standaloneTarget()` = 已知 id 快路径 + 通用路径；
  `insideManagedCard()` 拦住专属卡片的子层（否则蓝牙卡片会被叠两层）；
  `install(..., packageName, settingsSpecific)` 支持整体复用到其它 apk；
  `applyStandalone` 把「功能关闭」分支提到判定之前（关闭时零树遍历成本）；
  新增 `currentMaterial()` 供弹窗复用同一套色板。
- `appearance/SettingsPopupGlassHook.kt`（新）：MIUIX 弹窗（`PopupWindow`/`PopupView`/
  `HyperPopupWindow`）本来就有 `prepareMaterial()` 窗口玻璃实现，被三重门挡
  （主题属性 `menuBlurOptions`、`HyperMaterialUtils.isFeatureEnable`、包名白名单）。
  在「卡片材质 = 柔光玻璃 + `isBionicsActive`」时覆盖 `isMaterialEnabled()` 放行。
- `SettingsBackgroundHook.install()`：非设置进程安装通用卡片路由 + 弹窗材质
  （`CARD_MATERIAL_PACKAGES`，排除通控/短信避免与各自背景通道互拆图层）。
- `HookRuntime.moduleOrNull()`；docs/soft-glass-api.md §11/§12。

### 编译错误修复（K2 报 3 条）
- `CardSurfaceDetector.outlineRadius`：`ViewOutlineProvider.getOutline(View, Outline)` 和
  `Drawable.getOutline(Outline)` **都是 void**，`runCatching { ... }.getOrDefault(false)` 的
  `R` 被推断成 `Any`（`Unit : R` 且 `Boolean`）→「Condition type mismatch」。改为直接调用后
  读回填的 `Outline.radius`（provider 与 background 各用一个独立 `Outline`，避免互相污染）。
- `SettingsPopupGlassHook`：`private const val PREPARE_MATERIAL_CLASSES = arrayOf(...)` 非法
  （const 只允许基本类型与 String），改成 `private val` + `listOf`。

### 卡片路由的作用域扩展到「全部应用」（旧行 2026-09-24 追加）
- **LSPosed 机制（查证结论，重要）**：`META-INF/xposed/scope.list` 只是「推荐作用域」，
  不会强制生效，也**没有「全部应用」的通配写法**（LSPosed 只对 `system`/`android` 做系统框架映射）。
  实际生效范围 = 用户在管理器里勾选的应用。所以要靠代码层放开 + 用户全选配合。
- `SettingsBackgroundHook`：新增 `CARD_MATERIAL_ALL_APPLICATIONS = true`（默认全应用）、
  `CARD_MATERIAL_EXCLUDED_PACKAGES`（通讯录/短信=有自己背景通道会互拆图层、设置=DeviceModule 单独装、
  模块自身），新增公开入口 `installCardMaterialOnly()`。
- `HookEntry.onPackageLoaded()`：非官方支持清单的包不再直接 return，改为只装卡片+弹窗材质
  （不装背景/主题/文字色管线，避免改坏第三方应用界面）。
- **诊断日志（本轮新增，重要）**：`CardSurfaceDetector.probe(view)` 返回被拒的 `REASON_*`
  （原来的 `looksLikeStandaloneCard` 现在只是 `probe == null` 的包装）；
  `SettingsCardBackgroundHook.standaloneTarget()` 打 `Standalone matched key=…` /
  `Standalone near-miss <reason> class=… id=… size=…`，按「原因+类名+资源名」去重、单进程封顶 60 行。
  排查「某张卡为什么没生效」时只看 near-miss 行即可（尺寸不足/无背景不打日志）。
- 已知未解决（用户反馈这几张设置卡片仍无材质，待 near-miss 日志定位）：
  搜索历史里的小徽标（chip/徽标，高度可能 < 40dp 或被 negative hint 命中）、
  WiFi 详情状态卡、设置底部「在找其他设置吗」卡片。
- 性能提示：`setBackground` hook 在开启材质时会对每个「有背景且 ≥56×40dp」的 view 跑一遍 probe
  （含树遍历）；全应用作用域下这是主要开销来源，真机若出现卡顿优先看这里。

### 注意
- 未编译未测试（用户明确要求不要编译测试）。验证点在 docs 与 CHANGELOG 对应条目。
- 弹窗玻璃依赖系统 pass-window-blur 白名单；若真机上弹窗变「没有底」，
  先回退 `settings-popup:material-enabled:*` 这组 hook。
- 通用判定是启发式的：若某个页面出现不该上材质的卡片，优先给 `CardSurfaceDetector`
  的 `NEGATIVE_NAME_HINTS` 或尺寸门槛加词，而不是回到 id 黑名单。

## 安全中心「省电与电池」柔光适配（R8 压缩 apk 的结构发现）

用户反馈：加载安全服务 apk，省电与电池页有多张卡片，**颜色已能改、柔光上不去**。
jadx 反查根因（重要结论）：

1. **分组装饰器被 R8 改名**：MIUIX 内部类/方法在安全中心里成短名——
   `miuix.recyclerview.card.CardItemDecoration` → `miuix.recyclerview.card.f`（`C13600f`）、
   `PreferenceFragment$FrameDecoration` → `PreferenceFragment$f`（`C13532f`）、
   每帧绘制 `calculateGroupRectAndDraw` → `mo44927f`、裁剪 `clipDrawableRoundRect` → `g`、
   drawable 字段 `mGroupDrawable` → `f41500p` / `mCardGroupBackground` → `f40800s`。
   **公开名 `miuix.preference.PreferenceFragment` 与 `androidx.recyclerview...RecyclerView` 都保留。**
2. **颜色能改、柔光不能的原因**：`useGlass`/`useFrost` 都要求 `groupClipAvailable`，
   而它靠按字面名 `loadClass` + 找 `(Canvas,RectF,Path,Drawable)` 裁剪方法；
   压缩 apk 里类名不存在 → 抛异常 → 静默 false → 玻璃分支永久禁用。纯色分支不依赖它，所以颜色一直好使。
3. **省电与电池的自绘卡片**：`pc_battery_fragment_viewstub.xml` 用 `LinearLayout` +
   `selector_battery_card_bg` → `hp_card_bg_no_shadow_normal`（layer-list：全宽底 + 圆角半透明卡色），
   是**独立卡片**不是分组装饰；老的通用判定只读最外层 drawable → 拿不到圆角与填充 → 整片漏掉。

### 本轮改动
- `CardSurfaceDetector`（重写判定）：
  - `leaves()` 递归展平 `StateListDrawable`/`LayerDrawable`/`InsetDrawable`/`RippleDrawable`
    （ripple 只取第 0 层内容），形状与不透明度都基于叶子判定；
  - **先判形状再判不透明度**；新增 `MIN_SHAPE_SURFACE_ALPHA = 24`，形状确认为卡片时接受半透明卡面
    （`hp_card_bg`/MIUIX 卡色本身半透明），保留 `MIN_SURFACE_ALPHA = 190` 作不透明门槛；
  - `onTranslucentCard` 回调（诊断日志）。
- `SettingsCardBackgroundHook`：
  - `Access.factory` 改可空（结构发现路径可能没有工厂方法）；
  - 新增 `decorationClasses` / `hookedClipMethods`（去重）；
  - `installGroupClip` 改为「候选集合」：字面名 `BaseDecoration` + 所有已发现装饰器，
    按签名 `clipMethodOf` 定位、`hookClipMethod` 去重挂载；
  - `installDiscoveredDecorations()`：锚点 `miuix.preference.PreferenceFragment` 的内部
    `RecyclerView.ItemDecoration` + hook `RecyclerView.addItemDecoration` 运行时捕获；
  - `installDecorationHooks()`：必须能 `clipMethodOf != null`（排除普通分隔线），
    `structuralAccess()` 只看字段类型（`Drawable`/`Paint`）与工厂返回类型；
  - `installDiscoveredDrawHooks()`：按签名找每帧绘制方法（Canvas 开头、非抽象，优先 4 参且参数[1] 是 View）；
  - `hookGroupDraw` 归属判定改类型：`access.drawable.declaringClass.isInstance(owner)`；
  - 新增 `View.onSizeChanged` hook（`settings-cards:standalone-size`）：布局完成时补跑
    `applyStandalone`（attach/inflate 阶段 width=0 必然 too-small）；
  - 新常量 `BASE_DECORATION_CLASS`/`RECYCLER_VIEW_CLASS`/`RECYCLER_DECORATION_CLASS`/
    `DECORATION_ANCHOR_CLASSES`。
- 版本 1.4.4-beta10 → **1.4.4-beta11**（versionCode 73→74）；CHANGELOG 新增 beta11 段；
  docs/soft-glass-api.md：§11 判定表更新、§11.1 加 translucent 日志行、新增 **§11.2 容器 drawable 展平 +
  onSizeChanged**、新增 **§13 R8 压缩应用的分组卡片结构发现**、§9 补一句裁剪按签名定位。

### 结论（用户问：全局通用适配可行吗）
**可行**，但要满足四个条件（均已实现）：① 分组装饰器按结构发现（名字会变、结构不会变）；
② 卡片判定要展平容器 drawable；③ 接受半透明卡面（形状优先）；④ 布局完成（onSizeChanged）时补判。
唯一无法覆盖的是不走 MIUIX 也不自绘卡片面的页面（没有可接管的「面」），那本来也不需要适配。

### 注意
- 仍未编译未测试（用户要求）。真机验证点：安全中心省电与电池页的玻璃是否生效、
  `Card material runtime: clip=…` 与 `Discovered group decoration …` 日志。
- 运行时 `addItemDecoration` hook 里可能从非主线程挂 hook（一次性、PROTECTIVE），留意。
- 小徽标/WiFi 状态卡/「在找其他设置吗」三处仍未确认，看 near-miss 日志定位。

## DexKit 动态匹配方案评估（用户提议，结论：值得加，但只用于「发现」）

用户提出：不要按名字/签名匹配，改为用 DexKit 按「方法体里有什么（返回、参数、引用）」动态匹配。评估结论：**方向正确，且对多 apk/多页面/多组件比纯运行时反射更稳；但它是补强不是替代。**

### 概念澄清（写进文档时要保留）
- 目标实现本身就是 R8 之后的产物，**必须 hook 它**；不能做的是**用 R8 后的名字去认它**。
- R8 还会**内联**小方法 → 按名字找的目标可能根本不在 dex。DexKit 搜的是「最终 dex 里真实存在、且语义匹配的那个方法」，正好绕开内联。

### DexKit 2.x 能力（已核实）
`MethodMatcher`：`returnType`、`paramTypes`/`parameterTypes`、`modifiers`、`declaredClass`、`name`、`usingStrings`、`usingNumbers`、`usingTypes`、`usingFields`、`invokeMethods`（调用了哪些方法）、`annotations`。
`ClassMatcher`：`searchPackages`/`excludePackages`、`fields`(FieldsMatcher, addForType/count)、`methods`(MethodsMatcher, add/count)、`usingStrings`、`annotations`。
性能：链式 `findClass{...}.findMethod{...}` 比 `declaredClass` 条件快数十倍（官方性能指南）；避免在大 dex 上用 declaredClass。
关键：**锚点要用 framework 引用**（`android.graphics.Canvas#saveLayer`/`saveLayerAlpha`、`Path#addRoundRect`、`androidx.recyclerview.widget.RecyclerView$ItemDecoration`），这类名字 R8 不会改；用 app 类型名做 paramTypes 等于又回到名字依赖。

### hyperisland 参考实现（用户指定）
`C:\Users\1812z\Documents\GitHub\hyperisland\android\app\src\main\kotlin\io\github\hyperisland\xposed\hook\PermissionManager\ClipboardToastHook.kt`
- 依赖：`implementation("org.luckypray:dexkit:2.2.0")`（hyperisland app/build.gradle.kts:126）
- 两层策略：先按字面类名（`com.hyperos.security.utility.ToastUtil` 等）→ 失败再 `DexKitBridge.create(classLoader, false)`；`System.loadLibrary("dexkit")`
- 锚点是**方法体内的稳定字符串**（`usingStrings("showToastInternal  for ")`），再以广播 action 字符串锚定类；最后用**运行时形状**过滤（`isShowToastShape()`：2 参 (String,int)）+ `hasContextField`。
- 也演示了兜底：hook `WindowManagerImpl.addView` 拦截 TYPE_SYSTEM_ALERT。
- HyperBackground 目前**没有** DexKit 依赖（只有 libxposed api/service 102.0.0）。

### 安全中心实测（jadx 当前加载的就是安全服务 apk）
| 符号 | 结果 |
|---|---|
| `miuix.recyclerview` 包（含 CardItemDecoration） | **不存在**（整个包被 R8 重定位） |
| `clipDrawableRoundRect` | **不存在** |
| `miuix.preference.PreferenceFragment` | **存在**（锚点有效） |
| `androidx.recyclerview.widget.RecyclerView` / `androidx.preference.PreferenceFragmentCompat` | 存在 |
| 调用 `Canvas.saveLayer`/`saveLayerAlpha` 的类（安全中心全量只有 22 个） | 含混淆名 `p515u2.AbstractC15613b`、`p626y2.AbstractC16596h`、`p165ej.C10913a`，以及 `miuix.smooth.SmoothContainerDrawable`/`SmoothFrameLayout` |
→ 结论：分组装饰器的**名字全丢、行为还在**。这正是 DexKit 的用武之地：以 `Canvas.saveLayer/saveLayerAlpha` 为锚点 + framework 参数类型捞候选，再运行时按形状/归属过滤。

### 三层发现架构（推荐落地）
1. 字面名（未混淆/旧版，最快）
2. 运行时结构锚点（`miuix.preference.PreferenceFragment` 内部 `RecyclerView.ItemDecoration` + hook `RecyclerView.addItemDecoration`）——已实现
3. **DexKit 内容锚点（新增，兜底）**：一次进程扫描，命中结果按 `class#method#params` 缓存到 SharedPreferences；统一并入 `decorationClasses`，走同一套 hook 路径
DexKit **解决不了**「哪个 View 是卡片」（视图树是运行时问题）→ 独立卡片仍靠 `CardSurfaceDetector`（展平 + 形状优先 + onSizeChanged）。代价：apk 增加 `libdexkit.so`（4 ABI，几 MB）+ 每进程一次 dex 扫描（应异步 + 缓存）。

### 三张卡片的状态（用户本轮澄清）
- 搜索历史小徽标：**已正常**，无需处理。
- WiFi 编辑信息里的**大 card**：定位到 `com.android.settings.wifi.WifiConfigAdvancedLayout`（`R.id.advanced_options_layout` + `R.drawable.advanced_options_expanded_background` / `advanced_options_collapsed_background`）；每次 `setExpanded` 都 `setBackground(...)` 复用**同一个 Drawable 实例**（不是克隆），若我们 mutate 过它，展开/收起会复用被改过的实例 → 处理时要按状态分开。模块目前只处理了 `wifi_cardview_background_color`（在 `LIGHT_CARD_COLOR_RESOURCES` 里，属透明度路径）。
- 「在找其他设置吗」：**是回归不是发现失败**——`SettingsAppearanceApplier` 的透明度路径（`cardFinalColorReplacement` / `cardFinalDrawableReplacement` / `cardColor(opacity)`）在浅色模式且 `lightCardOpacity<100` 时把卡面 alpha 直接压到 `opacity*255/100`，opacity=0 → 卡面全透明。修法方向：给「特殊条目」加保护或设最小 alpha；待确认用户当时开的是哪个开关。

### 工具环境备注（重要，别再踩）
- WorkBuddy 的 Bash 工具在本机**完全不可用**（`dirname`/`ls`/`cd` 全 not found）。PowerShell 工具的 **stdout 不回显**，必须 `Out-File` 到文件再 `Read`。
- Read/Grep 对工作区外路径（如 `~/.workbuddy/projects/.../tool-results/`）会**权限拒绝**。
- `jadx-settings-out/` 只有 `.java` **没有 XML 资源**，定位布局/资源需在 jadx 里加载带资源的 settings apk。

## 「接管全部跟随主题的面」（用户要求：全部背景，含按钮 / card / 徽标）

用户诉求：**「只要有读取深浅色模式的背景，统统替换掉，包括按钮，card，徽标等等」**。

### 关键洞察（这是本轮最重要的设计结论）
用户这句话里的限定词「**只要读取深浅色模式的**」本身就是安全阀——它自动排除了
红点、品牌色按钮、图标、图片，而这些正是「全部替换」最容易搞坏的东西。
所以不需要枚举 id / 类名，只需要一个能回答「这块面跟不跟主题」的**行为判据**。

**不用「资源有没有 -night 变体」**（要把同一资源在两个 Configuration 下各解析一次，代价不可接受），
改用**合成后的中性色 + 主题侧亮度**：

```kotlin
ratio = alpha/255 ; base = if (night) 0f else 255f        // 当前主题的页面底色极性
r = (color>>16&0xFF)*ratio + base*(1-ratio)               // g / b 同理
chroma    = max-min  ;  > 24              → 品牌色，出局
luminance = (0.2126r+0.7152g+0.0722b)/255
night → lum ≤ 0.40   ;   day → lum ≥ 0.62 ;   中间带两边都不认（宁可漏不误伤）
```
**半透明必须按主题页面底色合成再判**：`#24FFFFFF` 这类卡底在深浅两个主题下 RGB 都是白，
直接看色值会得出「不跟随主题」的错误结论；合成后叠深色页仍是深色面、叠浅色页仍是浅色面 → 正确。

### 本轮改动
- `CardSurfaceDetector`：
  - 新增 `enum SurfaceScope { CARD, SURFACE }`；`probe(view, scope = CARD)`；
  - `SURFACE` 档：尺寸门槛降到 8×8dp（够徽标）、去掉形状要求、`THEME_NEGATIVE_HINTS`
    换一版（**移除** `badge`/`button`/`chip`/`tab`，保留 `icon`/`avatar`/`thumb`/`divider`/
    `scrim`/`mask`/`switch`/`checkbox`/`radio`/`track`/`overlay`/`dim`/`image`/`photo`/`logo`）；
  - 新增 `isThemeNeutralSurface()`（上面的判据）、`themeSurfaceFill()`、`primaryFillColor()`、
    `fillColorOf()`（ColorDrawable / GradientDrawable 实心色）、`coversWholePage()`
    （`PAGE_COVER_RATIO = 0.85`，整页背景交给整页通道）、`isNight()`；
  - 去重语义分档：`CARD` 用原 `hasOuterCardSurface`（拦任何更外层卡片面）；
    `SURFACE` 只拦「同一个面」（`background === view.background`，或子视图覆盖 ≥95% 且填充色相同）
    ——否则卡片上的按钮/徽标永远被拦掉；
  - 新增 `onThemeSurface` 回调（诊断）；
  - `leaves` 缓冲区改 `ThreadLocal` 复用（`withLeaves`，**不可重入**，文档已注明）。
- `SettingsCardBackgroundHook`：
  - `Palette` 新增 `allSurfaces`（默认 **true**）；新增 `surfaceScope()`；
  - `standaloneTarget()` 传 scope；`insideManagedCard` 守卫收窄为「CARD 档 + settingsSpecific」；
  - 材质不可用时的**回退**：`allSurfaces` 时不再回退透明（那会把按钮/徽标抹掉），
    改为回退所选浅/深色板实色填充；
  - 日志新增 `matched-surface key=` / `matched-surface fill=#AARRGGBB`。
- 设置项：`KEY_CARD_ALL_SURFACES = "card_all_surfaces"`（`SettingsAppearanceSettings` 读写 +
  数据类字段 `cardAllSurfaces` 默认 true）；`SettingsCardColors.kt` 加 `SwitchPreference`；
  `CardConfigCodec` 加 `allSurfaces`（导入/导出）；strings 中英各两条
  （`settings_card_all_surfaces` / `_summary`）。
- 版本 1.4.4-beta11 → **1.4.4-beta12**（versionCode 74→75）；CHANGELOG 新增 beta12 段；
  docs/soft-glass-api.md §11 加两档作用域表、§11.1 加两条日志行与新 REASON、新增 **§11.3 SURFACE 档判据**。

### 能力边界（写进文档的结论）
- DexKit「按方法体内容匹配」对**这一档没有帮助**——「哪个 View 是一块面、跟不跟主题」是运行时视图树问题，
  静态搜 dex 无能为力；DexKit 只在「找出被 R8 压掉名字的分组装饰器方法」那一半有用。
- 贴图型按钮（`BitmapDrawable` / `NinePatchDrawable`）拿不到可读填充色 → 一律跳过、不重新上色。

### 注意
- 仍未编译未测试（用户要求）。真机验证点：按钮/徽标是否上材质、`matched-surface fill=#…` 日志。
- 已知风险：深色主题下的纯黑遮罩（scrim）在亮度检验里与「纯黑卡片」无法区分，靠负向词与整页排除兜底；
  中间灰（亮度 0.40–0.62）两边都不接管，属刻意漏判。
- 行为变更：`allSurfaces` 默认 **true**，用户已有的「自定义卡片」开关一开即生效——若要回到只接管卡片，
  在 卡片背景样式 里关掉「接管全部跟随主题的面」。

### 工具环境备注（补充，重要）
- **同一个文件在一次消息里并行发多个 Edit 会丢改动**：工具报成功，但后续读回发现部分未生效
  （还出现过 `EBUSY: resource busy or locked`，疑似 IDE 占用/回写）。**同一文件的多次编辑必须串行**，
  改完用 Grep 逐条核实关键标记。
- 编辑后核实方式：Grep 关键标识（函数名/常量/新日志串），不要只信 Edit 的成功返回。

## beta12 编译错误修复（用户贴了 `compileDebugKotlin` 失败输出）

症状：5 条错误，全部来自上一轮「并行 Edit 丢改动」——**数据类字段与 import 没写进去，
但使用点写进去了**。这类错误有固定特征：报「Too many arguments」/「Unresolved reference」
的行号都在**使用侧**，定义侧静默缺失。

| 报错 | 真实原因 | 修法 |
|---|---|---|
| `SettingsCardBackgroundHook.kt:382 Too many arguments for Palette` | `Palette` 少了 `allSurfaces` 字段（12 个实参 vs 11 个形参） | 数据类补 `val allSurfaces: Boolean = true` + `preferenceKeys` 补 `KEY_CARD_ALL_SURFACES` |
| `SettingsCardBackgroundHook.kt:744/870 Unresolved reference 'allSurfaces'` | 同上（`colors.allSurfaces` / `palette.allSurfaces`） | 同上，一处修全清 |
| `CardSurfaceDetector.kt:285/286 Unresolved reference 'Configuration'` | 用了 `Configuration.UI_MODE_NIGHT_*` 但没加 import（之前写的是全限定名，后来改成短名） | 加 `import android.content.res.Configuration` |

**排查手法（可复用）**：不要逐个猜。先看报错行是「定义侧」还是「使用侧」——
若全是使用侧且集中在同一个新字段/新符号上，直接去定义侧 Grep 该符号，大概率是缺失而非写错。

### 顺带清掉的两条本文件告警（非阻塞，但都改干净了）
- `leafAlpha` 里 `drawable.opacity` 在 API 35 废弃 → 加 `@Suppress("DEPRECATION")`。
  **不要换判据**：`getOpacity() == TRANSPARENT` 是「这个 drawable 会不会画出东西」的唯一判据，
  改成 `alpha` 会把全透明的 `BitmapDrawable` 当成有效面。
- `ThreadLocal.withInitial { ... }.get()` 在 K2 下被当成**可空平台类型**（`MutableList<Drawable>?`），
  即使属性显式标注了 `ThreadLocal<MutableList<Drawable>>` 也不收敛 → 报
  「Only safe (?.) or non-null asserted calls are allowed」+ 两条 Java type mismatch。
  修法：`val parts: MutableList<Drawable> = leavesScratch.get() ?: ArrayList<Drawable>(8).also { leavesScratch.set(it) }`。

### 本轮结论（推翻上一轮的「未编译」状态）
**已用本机离线编译实测通过**：`:app:compileDebugKotlin` → `BUILD SUCCESSFUL`，
`CardSurfaceDetector.kt` / `SettingsCardBackgroundHook.kt` 零 error 零 warning。
命令（见 skill `android-gradle-offline-build`）：
```bash
export PATH="/usr/bin:/bin:$PATH"
export GRADLE_USER_HOME="C:/Users/1812z/AppData/Local/Temp/hb-gradle-home"
export GRADLE_RO_DEP_CACHE="C:/Users/1812z/.gradle/caches"
unset JAVA_HOME
./.gradle-dist/gradle-9.7.0/bin/gradle --offline --no-daemon --console=plain :app:compileDebugKotlin
```
- 本工程 Gradle 发行版在 `.gradle-dist/gradle-9.7.0/bin/gradle`（`build.sh` 里写死的版本）。
- 必须 `export PATH="/usr/bin:/bin:$PATH"`，否则 WorkBuddy 的 bash shim 里 `dirname`/`cd` 找不到
  （stderr 会刷 `dirname: command not found` / `cd: null directory`，**无害，可忽略**）。
- 只跑 `compileDebugKotlin` 约 12–20s（增量），足够抓类型/签名错误；**不需要** `assemble`。
- 仍未真机测试（用户只要求编译校验）。真机验证点：安全中心省电与电池页玻璃、
  `Card material runtime: clip=…`、`Discovered group decoration …`、`matched-surface fill=#…`。

## 架构盘点：设置卡片「动态 vs 写死」的现状（用户提问后的核实结论）

用户问「系统设置那些 card 是动态适配了还是依旧规则写死」。逐文件核实后结论：
**三条通道并存，两条已动态、一条仍写死，且动态与写死是叠加而非替换。**

| 通道 | 现状 | 具体位置 |
|---|---|---|
| ① 分组卡（MIUIX 列表） | **动态** | 字面名 `installRecycler`/`installPreference` 只是快路径；兜底是 `installDiscoveredDecorations()`：锚点 `DECORATION_ANCHOR_CLASSES = ["miuix.preference.PreferenceFragment"]`（公开名 R8 保留）内部的 `RecyclerView.ItemDecoration` + hook `RecyclerView.addItemDecoration`。裁剪方法按签名找（`clipMethodOf`），不按名字。`routingAvailable = recycler ‖ preference ‖ discovered > 0`（hook:203） |
| ② 独立卡（自绘背景） | **动态 + 设置进程 6 个写死 id** | `standaloneTarget()` 第 843 行 `if (settingsSpecific) knownStandaloneTarget(view)?.let { return it }`；`STANDALONE_CARD_IDS`（hook:1387）= `lock_screen_notification_card`/`float_notification_card`/`show_app_badge_card`/`device_basic_layout`/`device_params`/`view_corner`。**只有 `view_corner` 带真正专属逻辑**（`bluetoothRowOwnsSurface`），其余 5 个就是命中即返回。非集合内 → `CardSurfaceDetector.probe()` 动态；**非设置进程没有 settingsSpecific → 纯动态**（安全中心就靠这条自动生效） |
| ③ 卡片透明度 | **仍完全写死** | `SettingsAppearanceApplier`：`LIGHT_CARD_COLOR_RESOURCES`（15 个写死资源名，:566）+ `isCardLike()`（只看资源名/类名含 "card"，:1258）；`cardFinalDrawableReplacement`（:541）直接 `drawable.mutate().alpha = opacity*255/100` → **「在找其他设置吗」被清空的元凶**。靠 `managesStandalone(view)`（:530/:543）给②让位，但②没接管时会按写死表改 |

**两个关键澄清（写进回答，避免下次重复推导）**
- 通道③ **不是遗留代码**，它是「卡片透明度」这个开关的实现（`APPEARANCE_SLOT_HOME.lightCardOpacity`），
  与「自定义卡片材质」是两个独立功能，只是实现方式是写死资源名表 + 字符串启发式。
- ③ 与①②**不共享信息**：它只认那 15 个资源名，认得的就按 opacity 压 alpha，
  不管这块面是否已被动态通道接管过（只在 `managesStandalone` 为真时让位）。
  所以「动态」体现在**定位与接管**，不体现在**颜色/透明度改写**。

**若要彻底去写死，两个可收点**
1. `LIGHT_CARD_COLOR_RESOURCES` 15 个 → 可并入 `CardSurfaceDetector` 的 SURFACE 档
   （已有「随主题翻转的中性填充色」判据），用动态判定代替资源名表。这是「在找其他设置吗」的正解。
2. `STANDALONE_CARD_IDS` 除 `view_corner` 外的 5 个 → 通用判定现在应已能收，可逐个真机验证后删除，
   只保留 `view_corner` 的蓝牙身份判定（这个是必需的，不能纯动态）。


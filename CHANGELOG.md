# 版本说明

GitHub Actions 会按照 APK 的实际 `versionName` 提取对应章节，并写入 GitHub Release 描述。版本名包含 `test`、`alpha`、`beta`、`rc` 或 `dev` 时会自动标记为 Pre-release。

## 1.4.4-beta11

### 通用卡片与材质

- 修复：CardView 的表面透明度只按卡面底色判定——androidx 内部圆角填充恒报告半透明，曾让透明外壳冒充实心卡面，蓝牙页浅色模式下真正持白底的高亮层因此漏接管。
- 修复：独立卡片材质改为在绑定与布局时机同步应用，进入蓝牙等独立卡页面时原生白底不再先绘制一帧造成闪烁。
- 修复：通知与状态栏页的锁屏通知、悬浮通知、桌面角标说明卡恢复材质接管——负面名称过滤只拦类名，不再误拦含 badge 的整卡资源名。
- 维护：`local.properties` 移出版本控制并加入 `.gitignore`，本机 SDK 路径不再入库。
- 新增：内容驱动的通用卡片表面识别——在已知资源 ID 快路径之外，按视图尺寸、自有背景、卡片形状、表面透明度及外层卡面关系自动识别独立卡片，减少不同页面与应用逐项适配。
- 新增：支持递归解析 selector、layer-list、inset 与 ripple 等复合 Drawable，并接受具有明确卡片形状的半透明卡面；布局完成后通过尺寸变化补判，避免视图尚未测量时漏接管。
- 新增：卡片材质路由可扩展到 LSPosed 中实际勾选的其它应用；非官方支持范围仅加载卡片与弹窗材质，不启用整页背景、主题或文字颜色管线，并避开已有独立背景通道的应用。
- 新增：卡片背景样式加入「接管全部跟随主题的面」范围，可将卡片、按钮、徽标及分段底等随深浅色变化的中性表面统一接入所选材质；品牌色、图片与功能控件保持原样，材质不可用时回退所选实色而非直接抹除。
- 新增：MIUIX PopupWindow、PopupView 与 HyperPopupWindow 弹窗可复用当前柔光玻璃材质；仅在柔光玻璃模式及系统仿生材质、背景模糊条件可用时放行。
- 适配：安全中心「省电与电池」页支持 R8 混淆后的分组卡结构发现，按稳定类型与方法签名捕获装饰器、裁剪及绘制路径，使纯色、磨砂和柔光玻璃均可进入材质管线。
- 诊断：新增独立卡片命中、半透明卡面、主题表面及 near-miss 原因日志，便于定位尺寸、背景、形状、外层卡面或主题色判定造成的漏接管。

### 设置页面

- 修复：蓝牙已保存设备卡在列表绑定完成后接管系统后写入的高亮表面，浅色模式不再被不透明白底盖住；可用设备等普通列表行会撤回独立材质，避免与外层分组卡重复叠加柔光。
- 修复：设置搜索框保留 MIUIX 原生背景作为圆角轮廓，将配色交给柔光着色通道，并在视图完成挂载与硬件加速后补应用，恢复折叠状态下的柔光效果。
- 新增：主题商店「系统个性化」页文字卡接入当前卡片样式，支持柔光玻璃、磨砂、纯色及透明回退，同时保留 CardView 原生圆角。

### 配置与界面

- 新增：卡片材质页支持从剪贴板导出、导入完整卡片配置，并提供带确认的统一恢复默认；导入只覆盖卡片模式、明暗配色、模糊及柔光参数，不影响其它外观设置。
- 新增：设置页加入「配置」栏目，可在 HyperOS 设置首页插入 HyperBG 入口，自定义顶部、中部或底部位置，并选择是否与相邻项目同组。
- 新增：支持隐藏桌面图标；隐藏后 MainActivity 与 LSPosed 模块设置入口保持可用，仍可从 LSPosed 管理器或已启用的系统设置入口进入模块。
- 新增：关于页加入「请作者喝咖啡」入口与微信赞赏码页面。

## 1.4.4-beta10

### 设置卡片

- 新增：「我的设备」页四种自绘卡片样式（教程卡、COS 卡、Harmony 卡）及其信息子卡统一接入当前卡片样式——柔光玻璃、磨砂、纯色随设置生效；材质接口不可用时逐级回退，最终回退为透明。
- 优化：移除上述卡片写死的底色（教程卡灰底、COS 半透明白底、Harmony 蓝灰底等），卡面颜色完全由卡片样式配置决定，深浅色独立生效。
- 优化：COS 样式的参数大卡不再写死玻璃底色，统一由「我的设备」页独立卡片路由接管，与其它原生卡片行为一致。
- 优化：卡片样式配置变化时（改色、切换模式、关闭开关），所有已接入的自绘卡片自动同步刷新。

## 1.4.4-beta9

### 设置卡片

- 修复：通知与状态栏页的锁屏通知、悬浮通知、桌面角标卡片，蓝牙页的已配对设备卡片，以及“我的设备”页的参数大卡，统一接入当前卡片样式。
- 优化：上述独立卡片支持自定义颜色、磨砂与柔光玻璃，并保留系统原生圆角和轮廓；原生材质接口或轮廓无法应用时自动回退为透明背景，避免浅色模式显示纯白卡片。
- 修复：独立卡片由新材质路由接管时跳过旧的浅色卡片透明度处理；切换主题、材质或系统动态重设背景时会重新同步，关闭自定义卡片后恢复原生背景。
- 修复：蓝牙页“已保存”的单设备卡片改为直接接管 CardView 内部圆角填充并跟踪系统重绑；“我的设备”顶部设备信息卡补充精确布局命中，不再残留浅色模式纯白背景。
- 修复：独立卡片柔光玻璃与分组卡片统一颜色和参数合成路径——42 项材质参数同样清零 11–16 着色通道，调色后的 ARGB 由原生圆角背景绘制，避免相同设置出现偏色或透明度不一致。

## 1.4.4-beta8

### 设置卡片

- 新增：卡片背景模式增加「柔光玻璃」——在自定义颜色与磨砂之外，通过新的 `SettingsCardSoftGlassHook` 对分组卡片应用系统级柔光模糊，支持浅色/深色独立调节底色、模糊强度、不透明度和高光。
- 新增：柔光参数二级页（卡片材质）——柔光玻璃模式下可分别配置浅色/深色的底色、模糊半径、高光色、强度与不透明度；磨砂模式可配置磨砂底色与模糊强度。
- 新增：搜索框柔光材质——`SettingsSearchMaskOverride` 为设置主页搜索框独立应用柔光玻璃效果，与分组卡片的材质参数解耦。
- 优化：卡片背景 Hook 改为三分支分发——自定义颜色走纯色绘制、磨砂走模糊绘制、柔光玻璃走系统柔光接口，三种模式互不干扰。
- 修复：柔光玻璃切换时闪烁——统一复用已有绘制节点，参数变化时仅更新材质属性而非重建视图。
- 修复：顶栏静止时模糊层拦截触摸导致搜索框无法点击——改为在滚动停止时保留最小模糊表面，但将触摸事件透传给下层搜索框。

### UI

- 优化：Slider 组件交互与视觉。
- 删除：拨号盘缩放/焦点滑块入口（dialpad 背景尺寸已独立配置）。

### 通用

- 重构：`ConfigManager`、`InvalidatingCache`、`FirstFrameCallback`、`RootShell`、`XposedHooks`、`BackgroundDecodeSize` 统一迁入 `util` 包。
- 依赖：AndroidX Compose BOM、Core KTX 及 Miuix UI/preference/blur/icon 升级到最新版本。

## 1.4.4-beta7

### 设置卡片

- 新增：「设置主页 → 设置分组卡片」可启用自定义背景，浅色、深色各自保存颜色及不透明度，无需选择背景图片；关闭后重新解析当前系统主题的卡片背景。
- 新增：卡片背景提供「系统默认 / 自定义颜色 / 磨砂」三种模式；磨砂调用系统背景模糊接口，浅色、深色分别调节模糊强度、底色及不透明度，与纯色模式独立保存。
- 优化：磨砂沿用分组的原生矩形和圆角路径，在文字、图标下方绘制；复用可见分组的渲染节点，切换模式或退出页面时清理模糊状态，不支持时回退到所选底色。
- 适配：按设置 17 的反编译分析，直接处理首页／搜索 `CardItemDecoration` 与二级页面 `PreferenceFragment.FrameDecoration` 的分组底色，保留原有分组、边距、尺寸、圆角和点击前景。
- 性能：配置变更时缓存整组配色，绘制阶段不查询 ContentProvider；各装饰实例复用自己的 ColorDrawable，避免逐帧新建对象以及跨页面共享颜色缓存导致的漏更新。

## 1.4.4-beta6

### 拨号盘

- 基于回退后的 beta5 重新实现拨号盘背景层；自定义图片与默认模糊统一放在数字键下方，按实际键盘容器的边界测量、布局和裁剪四角，避免背景仅出现在左上角。
- 修复：原生背景可能通过 onDraw 自行绘制，仅替换 background 无法隐藏；改为保留原结构并控制背景 View 的 alpha，关闭或切换时还原。
- 优化：图片、模糊和底色采用统一圆角边界，跟随键盘位置变化；背景回收时清理布局监听与模糊状态。
- 调整：模糊开关和强度放在「拨号盘与列表」卡片，无需选择图片；默认模式模糊后方内容，自定义模式模糊所选图片，清除图片时保留模糊设置。
- 诊断：每次挂载记录一条背景模式、键盘边界和原生背景尺寸日志，便于定位不同通讯录版本的布局差异。
- 修复：仅在自定义背景模式下，将图片矩阵计算出的实际可见范围同步给圆角裁剪；四角边界和圆角半径随图片缩放及纵向定位一起变化。默认模式不受影响。
- 修复：默认与自定义背景曾使用包含额外内边距和控件的键盘容器作为公共范围，导致整体大一圈；两种模式统一改为跟随原生背景 View 的实际绘制矩形，自定义图片再在该矩形内缩放与裁切。
- 修复：根据通讯录 18.30.00.55 实包确认，原生背景 View 的 9-patch 外沿还包含约 12–13dp 阴影留白；两种模式改为运行时读取原生 Drawable padding，仅在实体面板内绘制与裁圆角，不再比原生轮廓稍大一圈。

## 1.4.4-beta4

### 我的设备

- 修复：自定义背景首帧尚未提交时过早拦截或停止系统背景 Shader，导致进入页面短暂露出黑底；图片和视频均等待首帧后再隐藏系统背景。
- 修复：视频准备完成就提前切换背景，改为等待 TextureView 收到实际视频帧；亮度遮罩同步延后显示。
- 优化：首帧回调随背景 View 销毁而取消，避免已退出页面的异步回调影响后续背景。

### 设置性能

- 优化：外观配置按通道缓存在设置进程内，通过配置监听与 ContentObserver 在修改后失效，避免资源加载和视图更新时反复同步查询模块的 ContentProvider；临时读取失败会自动重试。
- 优化：LOGO、卡片颜色和材质 Hook 先过滤无关资源与视图，减少普通设置页面的额外配置读取。
- 优化：背景图片解码移至共享后台线程池，按屏幕尺寸限制解码像素，并使用有容量上限的静态位图缓存；不同 View 不共享可变 Drawable 或动画播放状态，页面销毁后取消待处理任务。
- 修复：异步背景尚未就绪时重复生命周期回调提前隐藏系统背景；保持原背景直到新图可显示。
- 优化：模块顶栏模糊层仅在参数变化时更新厂商模糊接口，避免绘制回调重复设置；零强度或零透明度不再启用模糊，原生遮罩颜色未变时不重复触发重绘。
- 优化：Hook 参数数组按需复制，反射方法与字段查找缓存结果。
- 修复：Kotlin 反射辅助层的基本类型装箱匹配错误，避免 `setEnableBlur(Boolean)` 等调用匹配失败并在高频回调中反复抛异常。
- 测试：新增缓存失效、解码尺寸、反射查找与 Hook 参数传递的 JVM 回归测试；滚动帧率与各 HyperOS 版本视觉兼容性仍需真机验证。

## 1.4.4-beta3

### 短信

- 新增：短信背景通道（`com.android.mms`）。「短信」入口下分为两个二级配置——「设置主页背景」作用于会话列表、验证码/推广等分类列表与短信内部全部设置页；「设置聊天背景」作用于会话详情页（单/多收件人、RCS 聊天机器人、拦截会话）与新建信息页。聊天页未单独设置背景时自动跟随主页图。
- 新增：详情页「导出当前图片」改为下拉，可分别导出主页背景或聊天背景当前实际生效的图片（随机图优先）；随机背景作用范围同步支持短信主页、短信聊天两个槽位。
- 优化：会话/验证码列表的纯白条目沿用通讯录同款 `colorFilter` 递归透明方案（保留 selector 与 padding），配合 `View.setBackground` hook 与布局重扫监听即时清除回收重绑产生的白块；聊天气泡（白色/灰色收件气泡）与底部输入面板子树整树保护，不会被误清成透明。
- 修复：短信大量页面未重写生命周期方法、按类名 hook 时解析到共同基类同一方法，导致主页/聊天两组拦截器对同一实例交叉触发、聊天背景被主页图覆盖的问题——回调按实例真实类名精确守卫，两个通道互不干扰。

## 1.4.4-beta2

### 我的设备

- 新增：COS OS4「我的设备」样式（样式二）——顶部玻璃大卡（标题/副标题/签名 + 壁纸手机缩略图与实时时钟）、名称/存储双卡及参数卡玻璃化，沿用 overlay 挂载方式保留吸顶滚走动画；原样式二顺延为样式三；默认文案「HyperOS4 / 一千万内最好的系统 / Cangcu」可在模块设置内编辑。
- 新增：背景预加载开关——勾选后在设置进程启动时异步解码「我的设备」背景图入内存缓存，进入页面时第一帧即命中缓存同步显示自定义背景，消除黑帧；视频背景不预加载（需 SurfaceTexture），改用 `onReady` 回调兜底。
- 修复：进入「我的设备」页面黑背景一帧——原背景被立即 `INVISIBLE` 而新背景 drawable 尚未解码完成导致的空窗，改为等 drawable 就绪后再隐藏原背景。

## 1.4.4-beta1

### 通讯录

- 修复：联系人详情页（SubActivity / PeopleDetailActivity）无背景——反编译通讯录确认真实 Activity 类名后纳入通讯录背景通道，复用与主界面一致的背景层注入与列表适配逻辑。
- 优化：通话/联系人列表白底清除改用 `colorFilter` 递归透明方案——不再替换 background（会丢失 9-patch 的 padding/inset 导致内容贴边），而是对 StateListDrawable 的所有状态、LayerDrawable 的所有层统一设置透明 colorFilter，布局属性完整保留。
- 优化：新增 `View.setBackground` hook，item 重绑时即时清除不透明中性色底，避免等全局布局补扫的延迟白块；配合 OnGlobalLayout（设 dirty）+ OnPreDraw（执行清除）双监听器兜底，无闪烁且不卡。
- 修复：`view.context` 为 `ContextThemeWrapper` 时 setBackground hook 失效的问题——递归遍历 ContextWrapper 链查找 Activity。

## 1.4.3

汇总 1.4.3-beta6 至 beta8 全部测试线，作为稳定版发布。

### 通用

- 新增：随机背景功能——通过图片 API 随机拉取背景图，与手动设置的背景互不影响；独立存储 `<slot>.random.bin`，开关切换、重新换图都不会覆盖或清除用户手动选择的图片，未下载时自动回退手动图。
- 新增：图片来源可自定义 API 地址（默认 uapis 随机图片接口，需直接返回图片二进制流，支持 302 跳转）；提供图片类别选择——全随机 / 二次元动漫 / 风景图 / 混合动漫 / 手机壁纸 / 动漫图 / 电脑壁纸（平板，置末供平板用户选用）；二次元动漫类别默认强制手机竖图 `type=mb`，避免拉到电脑比例的横图。
- 新增：作用范围支持设置主页 / 我的设备 / 全局 / 通讯录 / 模块自身背景（拨号盘沿用整屏宽度渲染基准，随机图无法正确居中，已移除），默认折叠为一行、点击展开，summary 实时显示已选数量。
- 新增：槽位三态下拉——不使用 / 随刷新 / 固定。「固定」的槽位仍显示当前随机图，但跳过「立刻更换」与开机自动换图，图片保持不变。
- 新增：换图时机可选「仅手动」「仅开机自动」「手动 + 开机」；开机自动换图通过开机广播实现，接收器按所选模式动态启停——仅手动模式下开机不会拉起模块进程。
- 新增：导出当前图片——每个背景通道详情页的「设置背景」下方新增「导出当前图片」，将当前实际生效的背景图（随机优先，否则手动图）保存到相册 `Pictures/HyperBackground/`，并通知系统扫描。
- 说明：网络请求只在模块进程内完成，被 Hook 的系统进程不联网，仅读取已落盘的图片文件，渲染性能与手动选图一致；下载采用临时文件 + 原子替换，非图片响应、空响应、超限文件一律拒绝，失败时保留现有图片。

## 1.4.3-beta8

在 beta7 随机背景基础上，新增「导出当前图片」与槽位三态（不使用 / 随刷新 / 固定）。

### 通用

- 新增：导出当前图片——每个背景通道详情页的「设置背景」下方新增「导出当前图片」，将当前实际生效的背景图（随机优先，否则手动图）保存到相册 `Pictures/HyperBackground/`，并通知系统扫描。
- 变更：随机背景作用范围由开关改为三态下拉——不使用 / 随刷新 / 固定。「固定」的槽位仍显示当前随机图，但跳过「立刻更换」与开机自动换图，图片保持不变。
- 移除：随机背景作用范围中的「拨号盘」槽位——拨号盘沿用整屏宽度渲染基准，随机图无法正确居中；手动设置拨号盘背景不受影响。升级后自动清理历史配置。
- 优化：「立刻更换」只刷新未固定的槽位；全部固定或未选择时给出提示。

## 1.4.3-beta7

新增「随机背景」功能：通过图片 API 随机拉取背景图，与手动设置的背景互不影响。

### 通用

- 新增：随机背景——主页「作用域」卡片末尾新增入口，进入专门页面配置。开启后按所选作用范围使用 API 随机图，关闭开关即刻恢复手动设置的图片。
- 新增：随机图独立存储（`<槽位>.random.bin`），与手动背景文件完全隔离，开关切换、重新换图都不会覆盖或清除用户手动选择的图片；随机图尚未下载时自动回退手动图，不会出现空白。
- 新增：图片来源可自定义 API 地址（默认 uapis 随机图片接口，需直接返回图片二进制流，支持 302 跳转）；提供图片类别选择——全随机 / 二次元动漫 / 风景图 / 混合动漫 / 手机壁纸 / 动漫图 / 电脑壁纸（平板，置末供平板用户选用）。
- 优化：二次元动漫类别默认强制手机竖图（`type=mb`），避免拉到电脑比例的横图。
- 新增：作用范围支持设置主页 / 我的设备 / 全局 / 通讯录 / 拨号盘 / 模块自身背景，默认折叠为一行、点击展开勾选，summary 实时显示已选数量。
- 新增：换图时机可选「仅手动」「仅开机自动」「手动 + 开机」；开机自动换图通过开机广播实现，接收器按所选模式动态启停——仅手动模式下开机不会拉起模块进程。
- 说明：网络请求只在模块进程内完成，被 Hook 的系统进程不联网，仅读取已落盘的图片文件，渲染性能与手动选图一致；下载采用临时文件 + 原子替换，非图片响应、空响应、超限文件一律拒绝，失败时保留现有图片。

## 1.4.3-beta6

全模块 Kotlin 化与 UI 结构调整。

### 通用

- 重构：全部 Hook 逻辑由 Java 迁移至 Kotlin，统一使用 libxposed API 102 与自建 `XposedHooks.kt` 工具类，移除 de.robv 三件套依赖。
- 优化：反射方法/字段查找增加缓存，避免高频回调中重复遍历类层级。
- 优化：`HookRuntime` 方法参数懒加载，未访问时直接透传 `chain.proceed()`，减少不必要的数组拷贝。
- 优化：顶栏模糊滚动回调加 200ms 节流，降低浅色模式下的主线程负载。

### UI

- 变更：底部导航栏由两页扩展为三页——主页、设置、关于；「关于」页面从设置页的二级入口提升为独立底栏页。
- 新增：主页「一言」位置替换为模块状态卡片，显示 LSPosed 激活状态（已激活/未激活）及框架版本、API 级别；一言内容移至卡片下方。
- 变更：关于页应用标签卡改用 README 风格的 shields 徽章（release / license / Platform / Framework / ROM / Build），移除受 GitHub API 速率限制的 downloads 徽章。
- 优化：页面切换动画改为背景层不动、内容层滑动，消除整页连带背景左滑的突兀感。
- 修复：更新日志版本检查改用 GitHub `releases/latest` 的 302 重定向获取版本号，不再依赖 `api.github.com`，避免未认证请求触发 403。

## 1.4.3-beta5

修复全局二级页面与通讯录背景的图片缩放错位问题。

### 通用

- 修复：全局背景（设置二级页面）与「通讯录与拨号」整页背景此前一律按矩阵（MATRIX）以屏幕宽度为基准缩放，图片宽高比与视口不一致时会出现铺不满、错位。现改为与设置首页一致，无缩放参数的通道直接使用系统 `CENTER_CROP` 等比铺满并居中。
- 说明：缩放 / 定位矩阵仅保留给提供滑块的通道——设置首页（自身视口基准）与拨号盘自定义背景（整屏宽度 + 纵向位置基准）；其它通道不再挂布局监听，顺带省去逐帧布局回调开销。

## 1.4.3-beta4

性能优化：解决设置页面加载变慢、小米账号短暂显示「登录账号」的问题。

### 通用

- 优化：全局背景会话的 `OnGlobalLayoutListener` 补扫加 200ms 节流，合并页面加载时的高频布局回调，避免反复全树遍历。
- 优化：不透明中性色背景判定的 drawable 采样颜色按 `ConstantState` 缓存，同一次扫描内不重复创建 bitmap，减少 GC 与主线程耗时。
- 优化：外观子系统（移植自 HyperChanger）首页背景会话与卡片透明度会话的布局监听同样加 200ms 节流；卡片透明度、字体颜色模式未变化时跳过全树遍历。
- 优化：设置外观背景的图片解码由主线程移至后台线程，首帧只挂载空占位，解码完成后回填，避免大图解码阻塞主线程拖慢账号绑定等异步回调。
- 优化：首帧前仅做轻量的表面清除与背景挂载，全树遍历（表面清除 / 卡片透明度 / 字体色）延迟到下一帧，不再阻塞账号绑定。

## 1.4.3-beta3

重构「清除顶栏」实现，移除独立的顶栏强制隐藏 Hook，改为复用顶栏模糊管线。

### 通用

- 变更：「清除顶栏」不再通过独立 Hook 强制隐藏顶栏，而是复用顶栏模糊管线——开启时将模糊强度与透明度归零，使顶栏完全透明。
- 变更：「清除顶栏」开关由仅作用于设置首页，改为对设置各级页面（首页与二级页）统一生效，开启后覆盖顶部模糊效果。

## 1.4.3-beta2

恢复此前 `25ba622` 已实现、后续基线整理时被回滚的设置首页功能；其它功能保持 1.4.3-beta1 当前状态。

- 修复：设置首页背景恢复以自身视口为基准的 `CENTER_CROP` 等比铺满，默认 100% 缩放且居中时不再沿用拨号盘的整屏宽度定位。
- 恢复：设置首页背景缩放大小、横向位置和纵向位置调节，参数仅作用于首页通道。
- 恢复：「清除首页顶栏」开关；开启后首页透明顶栏优先，其它设置二级页的顶部模糊保持不变。

## 1.4.3-beta1

在 1.4.2 正式版基础上，仅新增背景亮度调节功能，其余保持不变。

### 通用

- 新增：背景亮度调节——每个背景通道（主页 / 全局 / 通讯录 / 拨号盘 / 设备等）各自独立的亮度滑块（0–200%，100 为原图不变，小于 100 变暗、大于 100 变亮）。图片用色彩矩阵调节，视频用遮罩近似。

## 1.4.2

汇总 1.4.2-beta1 至 beta11 全部测试线，作为稳定版发布。本版新增「通讯录与拨号」背景与拨号盘适配，以及移植自 HyperChanger 的「自定义我的设备 / 自定义 LOGO / 设备信息覆盖」外观子系统。

### 通讯录与拨号

- 新增：「通讯录与拨号」独立背景通道，主页新增卡片，选图方式与其它通道完全一致；通过 Hook `com.android.contacts` 进程注入背景，仅作用于拨号盘 / 联系人主界面（`PeopleActivity`），不影响联系人详情、编辑等二级页面。
- 新增：「拨号盘与列表」适配开关——遍历列表视图树，对「完全不透明中性色（黑/白/灰）」的背景层（联系人条目、列表容器、字母分组吸顶头等）替换为透明占位透出背景，深浅色行为一致；输入框、渐变遮罩、分割线等半透明层一律保留，条目随滚动复用时持续补清，关闭开关递归还原。
- 新增：拨号盘独立背景——详情页「拨号盘背景 默认 / 自定义」开关；选「自定义」时展开选图 + 透明度 + 清除组件，仅注入拨号盘键盘区，与联系人整页背景叠加共存；默认模式下键盘面板不透明度可独立调节，数字键始终清晰。
- 新增：拨号盘自定义背景缩放与定位——横向以屏幕宽度等比铺满并恒居中，纵向以拨号盘视口自身为取景范围（0=图顶对齐、50=居中、100=图底对齐），提供「缩放大小（1–200%）」与「纵向位置」滑块 + 数值输入框；仅作用于拨号盘背景通道，不影响联系人整页背景。
- 新增：拨号盘自定义背景圆角——`BackgroundMediaView.dispatchDraw` 内 `clipPath` 逐帧自绘四角圆角裁切。
- 新增：「通讯录颜色」独立深浅色控件——单独控制 `com.android.contacts` 进程的跟随系统 / 浅色 / 深色，与全局强制深浅色独立并存；「跟随系统」时主动撤销 per-app 夜间模式覆盖，避免锁死系统深浅色开关。
- 修复：进入联系人搜索时结果列表残留不透明白底——额外定位 Miuix `SearchActionMode` 拉起的搜索覆盖层并清除其内部不透明中性底。
- 说明：拨号盘背景仅支持图片；缩放 / 位置 / 透明度设置在拨号盘重新展开（`onFinishInflate`）时应用一次，调整后收起再打开即可生效。

### 自定义我的设备（移植自 HyperChanger）

- 新增：「自定义我的设备」入口取代原「我的设备」通道，内含设备界面样式、动态背景与自定义 LOGO。
- 新增：设备界面样式支持「系统默认 / 样式1（教程卡）/ 样式2（鸿蒙卡）」，样式1、样式2 可分别导入机型图片、背景图片、LOGO，并逐项调节缩放、偏移、模糊、行间距、对齐与文案等参数。
- 新增：「动态背景」通道（原「我的设备背景」并入），复用带预览的背景选图，静态背景支持透明度调节。
- 新增：系统默认样式下的「自定义 LOGO」——可选「不保留 / 保留高级材质（导入 SVG/XML）」并调节缩放；由「设备信息覆盖」页整卡迁入，避免重复入口。
- 新增：设备卡片页所有选图入口统一为「带预览对话框」，三种样式与主页背景选图体验一致，可在对话框内预览、换图、清除。

### 设备信息覆盖（仅显示覆盖）

- 新增：「设备信息覆盖」入口——可覆盖设置各页面显示的手机型号、处理器、运行内存、电池、屏幕、分辨率、摄像头、系统 / Android 版本、存储、内核、基带、硬件等参数，仅改变设置页显示，不修改任何系统属性。

### 通用

- 变更：升级 LibXposed API 至 102，以支持外观子系统所需的拦截链能力，原有背景注入体系不受影响。
- 说明：外观子系统以 `com.ciallo` 命名空间整套移植，与本项目原有背景注入体系并存；二级页参数滑块支持拖动实时跟手、松手落库。

## 1.4.1

本版本在 1.4.0 重构基础上修复深浅色残留、二级页进入闪烁与安全中心系页面底块残留，并补齐更新日志与更新提示。

- 修复：强制深浅色设为“跟随系统”后无法真正还原，导致设置等应用系统深浅色开关被锁死无法切换的问题——`onResume` 时始终撤销此前写入的 per-app 夜间模式覆盖（FOLLOW 显式设为 `MODE_NIGHT_AUTO`）。
- 修复：进入设置各二级页面时的黑屏/白屏闪烁——在 `Activity.onContentChanged`（首帧绘制前）同步挂载全局背景，让图片/GIF 背景随第一帧一起绘制。
- 修复：安全中心系页面（隐私与安全、应用设置等）顶部/统计卡残留的纯色不透明底块（深色黑块、浅色白块）；新增“不透明中性纯色面板”判定，仅清除完全不透明的黑/白/灰底块，半透明卡片与彩色控件一律保留。
- 修复：上述底块首次进入时残留、需进出子页面才消失的时机问题——统计卡为首帧后异步 inflate，改为在页面出现后一小段窗口内随布局变化补扫清除，窗口过后自动摘除监听。
- 新增：更新日志页面（关于 → 更新日志），并恢复检查更新，识别测试版并给出“发现新正式版 / 已是最新 / 检查失败”状态。
- 新增：检测到可更新的正式版时弹出全局更新提示对话框，显示更新日志并可跳转 GitHub Releases；取消后同一版本不再提醒。
- 新增：全局设置页“卸载 / 取消挂载前须知”警告卡片，指引取消作用域 / 卸载前先将颜色模式与文字颜色调回“跟随系统”、重启作用域并回访已挂载页面。
- 改进：重启作用域在 `am force-stop` 之外对常驻的 `com.android.phone` 追加一次 `pkill` 兜底，确保其被真正重启。

## 1.4.1-beta7

- 修复：安全中心系页面（隐私与安全、应用设置等）首次进入时，顶部/统计卡（如“位置/麦克风/相机”权限统计）残留的纯色不透明底块仍未被清除；此前需要点进子页面再返回才会消失。
- 定位：这些卡片是首帧之后异步加载/inflate 的，而原有清除逻辑只在 `onCreate`/`onResume`/`onContentChanged` 那一瞬间扫描一次，卡片此时尚未创建或未测量出尺寸，因而漏判；再次 `onResume`（从子页返回）时才重扫命中，故表现为“进出一次才生效”。
- 改进：为全局背景会话挂载布局观察者（`OnGlobalLayoutListener`），页面出现后的一小段时间内，每次布局变化都补扫一次并清除新冒出来的不透明中性底块；窗口过后自动摘除监听，长驻页面不产生额外开销。

## 1.4.1-beta6

- 修复：安全中心系页面（应用设置、隐私与安全等）顶部/统计卡容器残留的纯色不透明底块——深色下是黑块、浅色下是白块，此前未被清除、盖住了自定义背景。
- 改进：新增“不透明中性纯色面板”判定，仅清除完全不透明且黑/白/灰中性的 `ColorDrawable` 底块（深色黑、浅色白同时覆盖）；半透明卡片（如 `#24FFFFFF` 的 `GradientDrawable`/`CardDrawable`）与彩色控件一律保留，继续透出背景又保证可读。
- 回退：移除 beta5 的临时诊断日志代码。该诊断在取色时对共享 Drawable 调用了 `setBounds`，破坏了半透明卡片的绘制边界，导致 beta5 上半透明卡异常变透明；诊断移除后该副作用消失。

## 1.4.1-beta5

- 诊断：为定位安全中心系应用页（应用设置、隐私与安全等）残留的黑色/灰色卡片，加入一次性诊断日志，输出各卡片/大尺寸视图的 id、类名、尺寸与背景 ARGB 色值。
- 说明：本版仅用于采集色值数据，不含去黑块的正式修复；确定“黑 vs 灰”阈值后将移除诊断并实现正式逻辑（黑色去除、灰色半透明）。

## 1.4.1-beta4

- 优化：进入设置各二级页面时的黑屏/白屏闪烁。此前背景在页面绘制后才通过 `decor.post` 延迟一帧挂载，第一帧先露出系统原生底色（深色为黑、浅色为白）造成闪烁。
- 改进：在 `Activity.onContentChanged`（内容层刚创建、首帧绘制前）改为同步挂载全局背景，让图片/GIF 背景随第一帧一起绘制；同步失败时退回原异步兜底，最坏回到此前表现。
- 说明：视频背景因 `TextureView` 首帧异步准备，仍会有短暂占位空窗，本次不处理。
- UI：全局设置页“须知”卡片改为警告样式（蓝色高亮块），标题改为“警告”，正文精简为取消作用域 / 卸载前的四步操作指引。

## 1.4.1-beta3

- 新增：全局设置页新增“卸载 / 取消挂载前须知”卡片，提示先将颜色模式与文字颜色调回“跟随系统”、重启作用域并回访已挂载页面，再取消作用域或卸载模块。
- 新增：检测到可更新的正式版时弹出全局更新提示对话框，显示更新日志，可跳转 GitHub Releases；取消后同一版本不再提醒。
- 优化：更新日志页仅展示当前版本与最新正式版两节，不再罗列全部历史。
- 重构：抽出版本检查与 CHANGELOG 解析的公共逻辑，供更新日志页与更新弹窗复用。

## 1.4.1-beta2

- 新增：更新日志页面。从设置 → 关于 → “更新日志”进入，读取打包进 assets 的 CHANGELOG.md 动态展示全部历史版本。
- 恢复：检查更新功能，放在更新日志页面顶部。请求 GitHub releases/latest 与当前版本比对，识别测试版并给出“发现新正式版 / 已是最新 / 检查失败”状态，可手动重新检查。
- 改进：重启作用域在 `am force-stop` 之外，对常驻的 `com.android.phone` 追加一次 `pkill` 兜底，确保其被真正重启。

## 1.4.1-beta1

- 修复：强制深浅色设为“跟随系统”后无法真正还原、导致设置等应用的系统深浅色开关被锁死、无法切换的问题。
- 根因：旧逻辑在“跟随系统(FOLLOW)”时直接 return，从不撤销此前通过 `UiModeManager.setApplicationNightMode` 写入的 per-app 夜间模式覆盖，覆盖持久残留，即便停用模块或强杀进程也无法恢复。
- 改进：`onResume` 时始终调用 `applyApplicationNightMode`；FOLLOW 显式设为 `MODE_NIGHT_AUTO` 主动撤销 per-app 覆盖，DARK/LIGHT 分别设为 `MODE_NIGHT_YES`/`NO`。
- 恢复方法：启用模块并勾选作用域后，在 HyperBG 将强制深浅色设为“跟随系统”，逐个打开受影响的作用域应用（尤其是设置）触发 `onResume`，各进程会各自把 per-app 夜间模式还原为 AUTO。

## 1.4.0

- 重构：使用 Miuix 风格重构 UI
- 重构：迁移到 LibXposed API 101 实现

## 1.3.9-beta2

- 修复：重启作用域对部分包名（如电话、安全中心）失效的问题。旧实现把所有包用 `;` 串成单条 `su -c` 命令、并在 `waitFor` 之后才读取输出流，当某包输出较多写满管道缓冲时子进程阻塞、`waitFor` 超时，导致排在其后的包无法结束。
- 重构：参照 HyperIsland 的 RootShell，新增 `RootShell`（`su -c` 执行 + 独立守护线程实时排空 stderr，主线程读 stdout 后再 `waitFor`），避免管道阻塞。
- 改进：`forceStopScopes` 改为逐包独立执行并聚合结果，单个包失败不影响其余包；重启反馈区分“Root 不可用”与逐包成功/失败，提示“已重启 N 个，失败：xxx”。
- 强制深浅色：将主题（`SettingsThemeOverride`）与文字色（`TextColorOverride`）强制从仅 `com.android.settings` 进程放开到所有作用域进程，使应用详情页等由其它进程提供的页面也能被模块自定义深浅色控制。
- 测试重点为：作用域工具的常规/完整重启是否全部成功（失败会提示具体包名）；应用详情页及其它非设置进程页面在强制浅色/深色下配色是否跟随模块设置。

## 1.3.9-beta1

- 修复：应用详情页（应用信息）不跟随模块自定义深浅色、只跟随系统的问题。该页 header 卡片由 miuix 组件绘制，走 `ViewUtils.isNightMode(context)` 读取 Activity 实际 Resources 的 `Configuration.isNightModeActive()`，此前仅替换 `attachBaseContext` 的 Context 无法覆盖到它。
- 改进：在 `Activity.onCreate/onResume` 阶段直接改写 Activity 使用的 Resources 的 Configuration（uiMode NIGHT 位），使 miuix 组件的深浅色判定也跟随模块设置。
- 兜底：在设置进程通过 `UiModeManager.setApplicationNightMode` 统一改写进程级夜间模式，覆盖绕过 Activity Context 的组件。
- 测试重点为：应用详情页在强制浅色/强制深色/跟随系统三种模式下卡片与文字配色是否正确，以及其它设置页面配色与背景是否保持正常。

## 1.3.9

- 汇总 1.3.8 测试线的修复，作为稳定版发布。
- 修复：设置主页背景通道失效——`com.android.settings.MiuiSettings`（设置主页宿主）的生命周期 Hook 是 `applyHome` 的唯一挂载入口，早前误删导致浅色/深色两种主题下主页背景均不生效，现予恢复。
- 修复：设置搜索输入文字时整个页面短暂变黑，搜索加载层与窗口级搜索遮罩改为透明。
- 优化：还原桌面设置背景逻辑至已验证可用状态，保留旧版（OS3）桌面设置背景生效路径。
- 新增：关于页“本次版本说明”改为动态读取，按当前版本从打包的 CHANGELOG.md 自动解析对应条目；解析支持多种列表符号、跳过代码块并对版本号做归一化匹配，找不到精确版本时回退显示最新说明。
- 说明：LSPosed 模块描述扩写为功能概览，便于在模块列表中直观了解覆盖范围。
- 修复：模块仓库客户端读不到简介——`xposeddescription` 由资源引用 `@string/...` 改为写死的字面字符串（部分客户端只读 Manifest 原始值、不解析资源）。
- 兼容性：当前 HyperOS 桌面二级设置页由 Flutter/Rust 渲染，模块的 View 树背景注入对其无效；本版仅确保旧版桌面设置背景原本能生效的路径不被破坏。

## 1.3.8-test6

- 新增：关于页“本次版本说明”改为动态读取，按当前版本从打包的 CHANGELOG.md 自动解析对应条目；找不到精确版本时回退显示最新说明并标注实际运行版本。
- 优化：清理针对桌面设置的无效 Hook 残留（`MiuiSettings` 生命周期与 `com.miui.home` 原生 Activity 精确 Hook），这些通路经反编译与运行日志确认无法命中。
- 修复：还原桌面设置背景逻辑至 test5 可用状态，恢复 `home` 分支的短路判断，回退 test6 早期为适配 Flutter 桌面所做的改动。
- 兼容性：当前 HyperOS 桌面二级设置页由 Flutter/Rust 渲染，模块的 View 树背景注入对其无效；本版仅确保旧版（OS3）桌面设置背景原本能生效的路径不被破坏。
- 兼容性：仅调整桌面设置这一项，不影响设置搜索遮罩透明化等其它已生效逻辑。
- 测试重点为关于页版本说明显示、桌面设置页面背景、页面进入退出以及现有设置搜索功能。

## 1.3.8-test5

- 修复设置搜索输入文字时整个页面短暂变黑的问题，搜索加载层和窗口级搜索遮罩改为透明。
- 测试重点为设置搜索栏输入、搜索结果加载以及深色主题下的背景连续性。

## 1.3.8-test4

- 移除窗口重新获得焦点时的全局背景重应用，避免搜索栏获取焦点和软键盘弹出时触发背景清理造成黑屏闪烁。
- 移除全局布局监听器，避免搜索输入、结果刷新和键盘尺寸变化时反复递归扫描并清理整棵页面视图树。
- 全局背景仍在 Activity 创建、恢复和内容替换后按需挂载，保留生命周期调度去重。
- 测试重点为搜索栏连续输入、键盘弹出、搜索结果刷新以及二级菜单进入速度。

## 1.3.8-test2

- 修复电话服务“移动数据”二级页面的背景宿主判断，改为检查 `android.R.id.content` 的父级是否为 `ActionBarOverlayLayout`。
- 命中 MIUIX 转场容器时将自定义背景挂载到父级，避免背景跟随页面滑动并与旧页面短暂叠加形成虚影。
- 未命中已知层级时继续使用内容层宿主，保留不同 HyperOS 版本的兼容回退。
- 测试重点为进入和退出移动数据页面时的滑动转场、背景连续性及返回后的页面显示。

## 1.3.7-test3

- MIUIX 二级页面的全局背景仅在识别到 `ActionBarOverlayLayout` 时提升到其根层，不再触碰 DecorView。
- 状态栏、返回栏和大标题区域改为透明，背景图连续延伸到屏幕顶部。
- 直接清除 `ActionBarContainer` 内部绘制的 primary background，解决普通 `View.background` 清理无效的问题。
- 保留应用设置顶部功能卡、隐私页切换条和正文卡片的原生半透明样式。
- 已成功的设备互联与省电管理继续沿用内容层挂载，不进入顶栏提升逻辑。
- 测试版恢复正式包名 `com.ciallo.hyperbackground`，使用与正式版相同的私有 Release 签名并由 GitHub 标记为 Pre-release，可直接覆盖安装。
- 构建作用域校验由 8 项同步为 9 项，加入 `com.xiaomi.misettings`。

## 1.3.7-test2

- 回滚 1.3.7-test 错误的 DecorView 底层挂载，设备互联恢复为 1.3.6 已验证的 `android.R.id.content` 实现。
- 电话服务、小米账号、主题壁纸、手机管家等普通 Android / Miuix 页面统一使用内容层背景，并保留异步布局后的重复挂载。
- 新增 `com.xiaomi.misettings` 作用域，覆盖“健康使用手机”等由独立应用承载的页面。
- 增加 Instrumentation 生命周期兜底，处理未经过标准 Activity 基类回调的页面。
- 正式版与测试版的 Xposed 运行字段按包名隔离，避免两个模块同时安装时会话对象冲突。
- 新增 Hook 读取记录，可直接确认目标进程是否成功注入并读取全局背景。
- 系统桌面为 Flutter / Rust 共享运行时页面，本测试版保留注入诊断，不再使用会导致其他页面回归的通用 Surface 覆盖。
- 测试包继续使用 `com.ciallo.hyperbackground.test` 与调试证书，不覆盖或改动 1.3.6 正式版。

## 1.3.7-test

- 首轮多软件背景 Hook 兼容测试（已由 1.3.7-test2 替代）。

## 1.3.6

- 全局背景新增电话服务、小米账号、主题壁纸、系统桌面、手机管家与省电管理作用域。
- Settings 普通二级页面继续使用全局通道，设置主页与“我的设备”保持独立。
- 登录、授权、锁屏凭据、支付、拨号、紧急呼叫及浮动窗口保持系统原样。
- 调色盘升级为 12 色预设、HSV 色相/饱和度/明度滑杆和可编辑 HEX 输入。
- HEX 输入支持 `#RRGGBB` 与 `#AARRGGBB`，并使用 ASCII 键盘以便输入 A–F。
- 修复本地构建产物文件名没有跟随 APK 实际版本的问题。
- Release 构建改用 GitHub Actions Secrets 注入私有 PKCS#12 密钥；仓库不再保存签名文件或密码。
- 1.3.6 是私有签名迁移起点，与 1.3.5 及更早公开签名不兼容；从本版起保持签名连续。

## 1.3.5

- 移除背景页冗余的 HyperOS / Compose / MIUIX 介绍卡片。
- 关于页新增正式版版本验证、最新正式版状态和本次版本说明。
- 修复 Miuix 自定义主题色没有进入 Monet seed 配色流程、导致调色盘始终呈蓝色的问题。
- 正式版检查只读取 GitHub `releases/latest`，不会把 test / alpha / beta / rc 误判为正式更新。
- 包名和固定签名保持不变，可覆盖安装旧版。

## 1.3.3-test

- 完整移除失效的“主题通道实验”，包括配置界面、Provider 字段、Hook 入口与实现类。
- 全局背景继续通过 Activity 生命周期与页面根容器处理，不再拦截系统主题属性。
- README 仅保留当前正式版信息，测试版变更集中到本文件和 GitHub Releases。
- 构建链为正式版和测试版自动生成发行说明、构建信息与固定签名信息。
- 包名和固定签名保持不变，可覆盖安装旧版。

## 1.3.2-test

- 模块无自定义背景时，底色跟随 MIUIX 浅色或深色主题。
- 新增模块卡片透明度，并将原透明度选项明确为背景图透明度。
- HyperBG 大标题下新增“一言”区域，支持刷新、自定义 API 和 JSON 点路径字段。
- 保留主页、我的设备和全局三套独立背景通道。

## 1.3.1

- 配置界面迁移为 Kotlin、Jetpack Compose 与 Miuix KMP。
- 新增浅色、深色、跟随系统、Monet 壁纸取色和自定义主题色。
- 重新组织主页、我的设备和全局背景三通道。
- 保留媒体选择、透明度、模糊、文字颜色和 Settings 外观设置。
- 加入制作者苍簇、酷安与 GitHub 入口。
- 固定模块签名，后续版本可以覆盖安装。
